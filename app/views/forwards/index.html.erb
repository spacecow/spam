<%= title mess(:mail_forward_settings_for, user:current_user.userid) %>

<%= form_tag update_multiple_forwards_path, :method => :put do |f| %>
  <div id="flash_plus_button">
    <%= render 'layouts/flash', flash:flash %>
    <%= submit_tag t(:update) %>
  </div>
<% end %>
